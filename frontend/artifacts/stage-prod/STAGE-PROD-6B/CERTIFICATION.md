# NPA-T STAGE-PROD:6B — CERTIFIED

Existing valid Stage compositions now enter the existing STAGE-MOTION:1 path through an explicit read-only target projection. Position interpolation, bounded enter opacity/scale, retained-hidden exit opacity/scale, reduced-motion final-state fidelity, and canonical-ID safety are proven without a second animation engine.

## Representative transition

The focused tests construct an existing Capacity Gap Investigation composition and an existing two-Scenario Comparison composition. Both are valid STAGE-PROD:3 outputs. Their distinct immutable target fingerprints are handed to the same STAGE-MOTION:1 authority; the Comparison finishes with exact `ctx-scenario-pricing` and `ctx-scenario-demand` targets and no generated executive meaning.

## Verification

- Focused STAGE-PROD:6B A–J: 10 pass / 0 fail.
- Bounded STAGE-MOTION:1 + STAGE-PROD:3/5/6A/6B regression: 57 pass / 0 fail.
- ESLint on directly touched source/test files: pass with zero warnings.
- `tsc --noEmit --pretty false --incremental false` with an 8 GB Node heap: pass.
- Required Level 1 funnel: pass; 0 failed, 0 skipped, 0 running/uninspected required tasks.
- `git diff --check`: pass.

No Playwright, browser animation, long-session, FPS, or performance certification was run. Live Animation Playback debt remains OPEN. STAGE-PROD:6C has not started.
