# NPA-T STAGE-PROD:1 — Architecture inspection

## Production path discovered

`/executive` (`app/executive/page.tsx`)
→ `ExecutiveShell` → `ExecutiveCockpit` → `NexoraExecutiveShell` (NEX-MVP:2)
→ `NexoraStageMount`
→ `Nexora3DExecutiveStage` (NEX-MVP:3 host)
← Stage presentation from NEX-MVP:4 interaction + STAGE-PROD:0 productivity + STAGE-PROD:1 Queue
← Theatre from `projectNexoraDecisionTheatreFoundation` (DTH:1–12)
← Director intent from DTH `directorProjection` (DIR:1)
← NMI from `hostNmiLiveManagementIntelligence` (NMI:1–8)
← Advisor referent from `buildNexoraMVPAdvisorContextBridge` (CC:5)

`/executive/exs1` remains the development sandbox and is not the production Stage path.

## STAGE-PROD:1 addition

`projectStageProdLiveFoundation` is a read-only identity handoff onto the existing host. It does not compose a second Stage, Director, Theatre, NMI model, or Queue.

## Duplicate / obsolete paths

No second production Stage renderer was introduced. Demo/sandbox Stage remains at `/executive/exs1`.
