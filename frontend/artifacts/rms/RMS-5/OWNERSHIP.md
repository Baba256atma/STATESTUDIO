# NPA-T RMS:5 — Primary ownership

Earliest supported failure wins: SIMULATION → OPERATOR → DATA → MANAGER → CONVERSATION → NEXORA → RUNTIME.

Downstream findings keep their taxonomy but `primaryOwnership` points upstream. Operator gaps are not labeled as Nexora errors.
