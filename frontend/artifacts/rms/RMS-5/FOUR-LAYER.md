# NPA-T RMS:5 — Four-layer comparison

A Ground Truth, B Observable, C Nexora view/behavior, D Manager view/behavior remain separate `RmsLayerSnapshot`s. They are never collapsed into one truth state.
