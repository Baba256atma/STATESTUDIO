# Post-fork events

Cloned event schedules remain equivalent. External disturbances still fire on every branch when that branch’s clock advances. Management actions do not rewrite another branch’s schedule.
