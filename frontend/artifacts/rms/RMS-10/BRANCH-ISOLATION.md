# Branch isolation

After fork, Path A Ground Truth, clock, Operator publications, conversation bind, Stage subject on that bind, and Observer traces are not shared mutably with Path B. Proven by applying a modeled capacity action on one branch only.
