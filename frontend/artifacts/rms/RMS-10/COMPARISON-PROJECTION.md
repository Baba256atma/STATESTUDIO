# Comparison projection

Descriptive rows keyed by experimentId, branchId, runId, and operator-visible fields. `winner` and `bestPath` are always null. Language is simulated-consequences, not prediction.
