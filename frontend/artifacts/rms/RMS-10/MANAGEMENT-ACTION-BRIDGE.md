# Management-action bridge

Canonical management utterance (CC:5) may be referenced. RMS then maps an explicit Scenario action model through Operator-gated RMS:2 `applyRmsGroundTruthEventsOnCurrentTick`. Humans cannot edit Ground Truth keys directly.
