# NPA-T RMS:4 — Manager Objective contract

A session binds one bounded objective plus a deterministic agenda of RMS intents.

Northstar: understand delivery/capacity.

Warehouse Expansion: understand planned vs actual progress.

Objective + visible context + history + last Nexora response → next RMS intent → natural-language turn. Nexora still interprets the utterance independently.
