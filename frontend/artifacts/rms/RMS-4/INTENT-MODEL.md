# NPA-T RMS:4 — Manager intent model

Intents: UNDERSTAND, INSPECT, INVESTIGATE, ASK_DATA, ASK_CAUSE, ASK_OPTIONS, COMPARE, ASK_RECOMMENDATION, FOLLOW_UP, CLARIFY.

These are simulated manager desires. They do not replace CC:1 intent resolution. Tests assert RMS intent ≠ Nexora `intentResult.intent.kind`.
