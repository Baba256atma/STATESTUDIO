# NPA-T RMS:4 — Mutation / confirmation boundary

`autoConfirm`, `confirmationBypass` are false.

A Manager `yes` is sent through CC:5 unchanged. RMS does not auto-confirm add/remove, Object mutation, semantic confirmation, Decision approval, or Execution start.
