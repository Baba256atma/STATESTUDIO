# Fork / EXPERIMENT compatibility

`forkCompatible: true` on definitions and run results. Independent `runId` per execution. Branching is not implemented. RMS:8 WATCH / later EXPERIMENT remain out of scope.
