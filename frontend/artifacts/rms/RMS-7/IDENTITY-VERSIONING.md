# Identity and versioning

Stable `scenarioId` (kebab-case) plus `version` (`major.minor`).

Example: `manufacturing-capacity-pressure` @ `1.0`.

Registry resolves by id and version. Not a package manager.
