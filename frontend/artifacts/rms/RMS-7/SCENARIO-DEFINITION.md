# Scenario Definition

Canonical type: `RmsScenarioDefinition` in `rmsScenarioContract.ts`.

A Scenario is world + conditions. It configures RMS:2–6. It does not replace those engines and does not encode a correct Nexora answer.
