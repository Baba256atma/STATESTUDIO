# Stage integration

Stage panel reads `focusedSubjectId/label` and `workspace` from the real CC:5 runtime captured during the Scenario run. RMS:8 owns no Stage objects. If CC:5 does not change attention, WATCH does not fake a change.
