# Observer privacy firewall

WATCH uses `exportRmsWatchSafeSurface`. It does not include Ground Truth, event schedules, Observer reports, or conversation error taxonomy. Observer still runs inside `executeRmsScenario` for certification.
