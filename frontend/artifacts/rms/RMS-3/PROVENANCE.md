# NPA-T RMS:3 — Provenance

Nexora-facing chain:

Data Reality fact provenance ← RDI source record (`source:rms-operator:{runId}`) ← Observable recordId / field / tick ← Operator observation action.

Observer-facing chain additionally includes sealed Ground Truth and Operator ledger.

Provenance does not include internal Ground Truth keys (`availableCapacity`) or causal claims.

Observer may compare: what was true vs what was observable vs what reached P0:1.
