# Ground Truth access audit

`inspectRmsGroundTruth` requires OBSERVER. Manager Agent, Human Manager, and Nexora throw. Apply/pause require OPERATOR_AGENT.

WATCH presentation and Experiment comparison are leak-tested against hidden keys and Observer taxonomy. Comparison uses Operator field names, not `availableCapacity` / `machineAvailability`.
