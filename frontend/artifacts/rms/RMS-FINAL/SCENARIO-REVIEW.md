# Scenario review

Four scenarios, version `1.0`, one registry/runner:

- manufacturing-capacity-pressure (BUSINESS, DEEP)
- project-delivery-pressure (PROJECT, DEEP)
- logistics-delivery-pressure (BUSINESS, PARITY)
- service-capacity-pressure (BUSINESS, PARITY)

Forbidden keys (`expectedProblem`, `correctDecision`, `winningScenario`, …) are rejected. Manager-visible context does not include hidden events. HYBRID is template instantiation only (`hybrid-reserved`), not expanded in FINAL.
