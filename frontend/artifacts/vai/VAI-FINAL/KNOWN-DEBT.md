# NPA-T VAI:FINAL — Known debt

Bounded. Does not violate VAI:1–8 or FINAL gates.

| Debt | Description |
| --- | --- |
| A | Live `/executive` still requires the host to supply `vaiAdvisorBundle`, what-if session, trusted models, and VAI:8 promotion session. Missing bundle invents nothing. |
| B | Trusted quantitative models remain caller-supplied `VaiTrustedQuantitativeModel` records. No plant-model catalog is owned here. |
| C | Multi-variable interaction has no trusted combined model; unsupported interaction stays unknown. |
| D | DIRECTOR-1:1 file-list test (`import.meta.dirname`) is undefined under the `tsx --test` harness. Not a VAI truth/writer defect; DIR:1 foundation identity tests still pass. |
| E | Pixel-level Theatre lever clicks remain host UI. VAI:7 exposes CURRENT vs WHAT-IF presentation tokens only. |
| F | Future capabilities (optimization, causal model learning, coefficient estimation, forecasting, automatic Decision) are out of program. Not started. |
