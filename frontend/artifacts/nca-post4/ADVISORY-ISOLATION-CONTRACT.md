# Advisory Isolation Contract

Historical advisory state is thread-scoped, not generic truth. It participates only when its option belongs to the resolved candidate family and the turn is semantically advisory. Incompatible Scenario advice is suspended during Problem comparison, not deleted, so legitimate NCA:2 thread return can restore it.
