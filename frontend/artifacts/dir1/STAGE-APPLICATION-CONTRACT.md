# Stage Application Contract

`applyDirectorPlanToStage` is the controlled conversation-driven application path. Focus delegates to `selectNexoraMVPInteractionSubject`. Collection writes the existing `collectionContext`, clears semantic focus, and leaves canonical objects and business stores untouched. Already-satisfied and empty plans preserve state.
