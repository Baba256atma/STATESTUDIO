# Reference and composition evidence

Machine fields: isolated `referent`, `compositionResolved`, `compositionSelected`, `focusedLabel` in `runtime-turns.json`. Live: `focused`, `moActive`, reply text.

## Closed on this run

- Stage click away → Capacity Gap → explain / tell me more: composition Problem / Capacity Gap.
- Demand Surge → explain / tell me more: composition Scenario / Demand Surge (isolated and live).
- Explicit named Expansion Plan outranks continuity (live MO moves to `ctx-scenario-capacity`).

## Open S1

Live INVESTIGATE-family after Demand Surge: Stage/MO still `ctx-scenario-demand`; composed answer is Margin Pressure (Problem). Isolated composition for the same utterances stayed `scenario` / Demand Surge.

Related objects may contribute intelligence. They must not replace the manager’s established subject. Here a **different Problem** replaced a Scenario on live INVESTIGATE, which is stricter than the historical related-Scenario steal.

## Supporting isolated traces

J4: CSV `explain it` answers the file even when referent still names a Scenario. Manager-visible subject is CSV.  
J4 after HELP: `explain it` clarifies Problem vs KPI instead of guessing.
