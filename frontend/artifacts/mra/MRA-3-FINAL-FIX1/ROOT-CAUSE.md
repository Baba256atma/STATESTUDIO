# Root cause

MRA-3-FINAL-001 was a **live integration/parity** defect, not a missing Capacity Gap / `explain it` phrase rule.

1. **Competing Chat path.** Live Chat returned from DATA-ADV without CC:5. Isolated CC:5 is the only path that keeps NCA, FINAL:6.2 continuity, and executive context aligned after a unique CSV inventory. The older Scenario member remained an effective pronoun target.

2. **Duplicate Runtime dispatch vs Stage.** Command ids are target-stable. After a click moved Stage, the same focus command was skipped (`Already on Capacity Gap`) so Stage/executive current stayed on the clicked object.

3. **Scenario explanation fidelity vs current referent.** With a stale presented Scenario set, deictic `explain it` was rewritten to `explain-scenario` even when CC:7 current subject and FINAL:6.2 continuity were the named Problem.

No Object-specific production repair. No CSV→Problem hard-coded transition.
