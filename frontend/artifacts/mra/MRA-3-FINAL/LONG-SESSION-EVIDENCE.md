# Long-session evidence

**53 manager turns**, one CC:5 conversation, no reset (`LONG-52` in `runtime-turns.json`).

Covered: Stage, Problems, Data/CSV, KPI/Capacity, Risk add attempt, Scenarios, recommendation/criterion, Decision, Execution, correction, unrelated HELP, return to first problem.

## What held

- `look at capcity` → Capacity Gap.
- Scenario list → CSV inventory → `explain it` → CSV (pending, unresolved fields, no invented meaning).
- Ambiguous `it` after HELP clarified problem vs KPI rather than guessing.
- `Approve Demand Surge` wrote canonical Approved=1; `start it` wrote Execution=1.
- Topic-change after unnamed Risk add did not mutate on `yes`.
- Delete Margin Pressure did not become Add.
- Cause question treated as hypothesis, not proof.

## Friction (not patched)

- After CSV, `ok Capacity Gap` was not accepted as a clean subject switch (`I’m not sure how that relates`).
- After `go back to the first problem` (Capacity Gap listed), `explain it` returned scenario-projection language instead of Problem explanation (T53). Isolated J3 still explains Capacity Gap.
- Approval sentence in the long session also said “Capacity Gap is already the committed Decision” while Demand Surge was the canonical Approved option (leftover overlay; counts remained 1).

These are classified in FAILURE-MAP.md. They did not fabricate Execution or write a second Decision.
