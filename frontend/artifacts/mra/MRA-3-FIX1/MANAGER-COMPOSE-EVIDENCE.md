# MRA:3-FIX1 — Manager Compose Evidence

Path that leaked: ECA execution notes and attention labels appended after NCA:6, plus clarification option ids.

Fixes consume existing NCA:6 / attention composer: re-apply NCA:6 after ECA overlays; manager-facing subject labels; `INSUFFICIENT_REALITY` not spoken as a name; leak scanner does not treat WATCH as an internal token (that was mangling Overview actor names).

`runtime-turns.json` `leaks: []` for the MRA:3 leak regex.

`Should we change the plan?` no longer contains `ECA:10`. Weather / `what about totalCapacity` no longer speak `INSUFFICIENT_REALITY`.

Unseen: `mra2ManagerReadiness` leak test plus weather follow-up.

Developer diagnostics may still contain enums.
