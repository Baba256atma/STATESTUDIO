# Authority path

Input
→ CC:1 (compare overlay / unknown; `prolems` not rewritten)
→ POST:1/3 active Problems membership
→ POST:4 `interpretExecutiveComparisonMeaning` (`criterionAmbiguous` when `important` and no named criterion)
→ NCA:3 `buildNca3ComparisonCriterionClarification`
→ NCA:2 pending `comparison-criterion` / `PRIORITY` + `activeComparison` subjects
→ short criterion answer (`urgency`) via existing NCA:2 `extractAnswer(PRIORITY)`
→ POST:4 criterion URGENCY + NXA:5 criterion-specific insufficiency
→ NCA:5/NXA:5 continuation (explicit other criterion, e.g. evidence strength)
→ DIR NO_CHANGE
→ Stage preserved

No second comparison, clarification, pending, evidence, Advisor, or Stage authority was added.
