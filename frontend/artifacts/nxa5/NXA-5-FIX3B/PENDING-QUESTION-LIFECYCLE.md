# Pending-question lifecycle

Ambiguous importance
→ NCA:3 criterion question
→ NCA:2 `pendingQuestion.purpose = comparison-criterion`, `expectedInformation = PRIORITY`, `status = ACTIVE`
→ `activeComparison.candidateIds` = Capacity Gap + Margin Pressure, `criterion = UNSPECIFIED`

`urgency`
→ NCA:2 ANSWER_NEXORA, lastAnswer kind PRIORITY
→ pending cleared
→ POST:4 URGENCY on the same IDs

`show decisions` during pending
→ not ANSWER_NEXORA, lastAnswer remains null, intent show-decisions (B9)

No second pending store.
