# NXA:5-FIX3B-DIAG2 — Background-task ledger

Required: started 5, passed 5, failed 0, running 0, uninspected 0.

Barrier allowed. See `background-task-ledger.json`.

Nonessential: existing Next.js `node` PID 62339 on port 3000 (not started, not stopped).

`nxaConversation` returned to default-off after the executor (`full-conversation-traces.json` `nxaConversationAfterDisable: false`).
