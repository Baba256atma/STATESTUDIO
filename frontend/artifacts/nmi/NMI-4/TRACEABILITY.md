# NPA-T NMI:4 — Traceability

Every displayed element traces to:

- canonical ID (`elementId` / `canonicalRef.id`)
- canonical authority
- NMI relationship IDs when present
- provenance from the Management Map / comparison sourceRef

There is no opaque NMI-created truth. Sealed RMS facts are rejected.
