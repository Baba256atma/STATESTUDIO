# NPA-T NMI:4 — Next-transition semantics

`possibleNextStages` lists structurally supported following stages.

Each item is `descriptive: true` and `requiredAction: false`.

NPS / ECA / Advisor remain responsible for guidance. NMI:4 does not prescribe actions.
