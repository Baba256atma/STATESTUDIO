# NPA-T NMI:8 — Browser / Runtime Evidence

This environment has no authenticated browser MCP / live click-through harness for `/executive`.

Certified substitutes:

- Overlay SSR with live `mapNodes` (`data-nmi-live="8"`, Capacity Gap node, section counts)
- CC:5 `executeNexoraConversationalExperience` with the live host bundle (Journeys A–E)
- Stage handoff through `composeNmiStageProjection` + `selectNexoraMVPInteractionSubject`
- Shell source wiring: `hostNmiLiveManagementIntelligence` and `nmiAdvisorBundle`

Classification: **environmental limitation** — live browser click-through not available; runtime conversation/Stage/overlay paths were executed in Node. SSR-only overlay is not the sole evidence.
