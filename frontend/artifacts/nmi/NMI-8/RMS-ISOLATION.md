# NPA-T NMI:8 — RMS Isolation

`nmiLiveHost.ts` does not import RMS Ground Truth.

`simulationStateIds` on the live model is empty.

Sealed RMS facts remain `rejectSealedRmsAdvisorFact` unknown/unspoken.

Demo/simulation may enter NMI only through legitimate catalog/canonical channels.
