# NPA-T NMI:6 — Epistemic safety

Projection copies NMI:3 `causalCommunication` and NMI:1 epistemic status.

Association / hypothesis / analytical influence are not upgraded to confirmed cause.

`causal: false` and `convertsAssociationToCause: false` on every projected relationship.

Unresolved evidence (`UNKNOWN` / `unresolvedRelationshipIds`) remains unresolved.

Missing canonical relationships become `relationshipGaps`. They are never fabricated as Stage edges.
