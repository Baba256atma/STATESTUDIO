# NPA-T NMI:6 — UI scope

Minimum proof surface:

- Overlay `data-nmi="6"`
- Optional `projectionAnchorId` from existing Stage focus
- Optional map node buttons calling `onSelectCanonicalId` → existing `onSelectSubject`
- Queue rows / testids unchanged

Not in this phase: Bubble Scene, Portfolio chart, new chart engine, 3D objects, new animations, DTH-EXP scene language, NMI-aware Advisor answers (NMI:7).
