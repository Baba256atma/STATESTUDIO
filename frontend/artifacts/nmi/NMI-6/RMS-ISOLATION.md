# NPA-T NMI:6 — RMS isolation

Sealed RMS Ground Truth does not leak through Stage projection.

`rejectSealedRmsProjectionFact` returns `known: false`, `displayed: false`.

Participant contract remains `groundTruthAccess: FORBIDDEN`.
