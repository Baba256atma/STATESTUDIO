# NPA-T NMI:6 — Decision Roadmap → Stage

NMI:4 may contribute ROADMAP-tier context.

Do **not** display the entire company journey by default.

Selected External Capacity Scenario (example): Capacity Gap → that Scenario → comparison ref → Decision state — only if those IDs are relationship-supported and inside the bounded branch.
