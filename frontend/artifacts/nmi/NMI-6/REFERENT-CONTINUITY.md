# NPA-T NMI:6 — Referent continuity

NMI context must not steal conversational subject ownership.

Sequences certified here and by NCA:1:

- Select Capacity Gap → Explain it → still Capacity Gap
- Select Demand Surge → Tell me more about it → still Demand Surge
- Select Capacity Gap → project related Scenarios → Investigate it → still Capacity Gap unless the manager selects a Scenario

`nmiProjectionReferentForUtterance` returns the current canonical ID; collections are context only.
