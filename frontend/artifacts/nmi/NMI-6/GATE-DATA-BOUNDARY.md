# NPA-T NMI:6 — Gate / Data Reality boundary

```
Input → Gate API (RDI:1) → canonical authority / Data Reality → NMI → Stage context projection → existing Director/Stage writer
```

Stage interaction through NMI:6 must not bypass Gate, confirm semantics, create evidence, upgrade evidence, or write Data Reality.

`NMI_GATE_BOUNDARY.nmiGate === false`.
