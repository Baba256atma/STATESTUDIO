# NPA-T NMI:7 — NMI Advisor context contract

`NmiAdvisorContext` is session-scoped read context, not conversational memory and not canonical state.

Includes: active canonical subject, management context, bounded branch, interpretations, gaps, Decision Roadmap summary, Attention item, Stage projection anchor, evidence/provenance, unresolved IDs, possible next stages (`requiredAction: false`).
