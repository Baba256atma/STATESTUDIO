# NPA-T NMI:7 — Advisor authority map

```
Manager utterance
  → CC:1–7 / NCA / ECA  (conversational authority)
  → resolved canonical subject
  → NMI:7 read context (optional bundle)
  → applyNmiAdvisorToPresentedResponse
  → existing presentedResponse
```

CC:5 remains the sole Advisor composer. NMI is not `NmiAdvisor` as a competing system.

VAI:4 still owns Variable/causal overlay when it applies; NMI does not replace that CAUSAL overlay.
