# NPA-T NMI:1 — Gate API boundary

Canonical Gate: **RDI:1** `NexoraRealDataIntegrationFoundation`.

Flow:

External/Internal Input → Gate API (RDI:1) → canonical authority / Data Reality → NMI Unified Management Model.

NMI does not introduce `NmiGate`, a second ingestion gateway, a parallel validation API, NMI-specific Data Reality, or a bypass of admission rules.
