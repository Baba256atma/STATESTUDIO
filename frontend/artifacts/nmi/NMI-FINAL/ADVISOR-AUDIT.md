# NPA-T NMI:FINAL — Advisor Audit

NMI:7 remains an overlay/read context inside CC:5 / NCA / ECA.

- no second Advisor
- no second referent resolver
- NCA retains subject ownership
- explicit named switch wins (`Explain Margin Pressure.` then related)
- NMI management questions work (related, Attention, roadmap, missing)
- unrelated conversation remains existing CC:5
- commitment/write turns remain with existing authorities

## Identity repair required for FINAL journey

The phrase `Where are we with this problem?` previously resolved to catalog-first Problem alias **Margin Pressure** while Stage remained on **Capacity Gap**. That is an NCA/NLU identity failure, not an NMI Map bug.

Bounded repair (existing NCA path, not a second NMI resolver):

- deictic follow-up includes Attention / “where are we with this (problem)” / “what is missing”
- generic kind aliases skipped when preceded by this/that
- `findObjectMentions` does not recover a registered Problem from the token `problem` when the original utterance contains `this|that problem`

After repair, the FINAL journey keeps Capacity Gap until the explicit named switch.
