# NEX-EXP:8 Start Contract

PLAN REVIEW ≠ START EXECUTION.

“I like this plan.” does not start execution.

Start intent opens confirmation. Only then: CC:11 `createExecution` / `prepare` / `start`.

Missing adapter: start is refused, not faked.
