# Grounded Scenario Impact contract

CC:9 remains Scenario authority.

Conversation produces intervention semantics.
`assessGroundedScenarioImpact` binds interventions to **canonical subject primary KPIs** from presentation fixtures (semantic, not Stage topology).

Impact basis for Delivery delay: `canonical-subject-kpi`.

No numeric magnitude from intensity.
Projection ≠ Reality / Outcome / Learning / causal proof.
Unsupported remains valid when no canonical KPI/profile exists.

Assessment types: `GroundedScenarioImpactAssessment` and `GroundedScenarioImpactTarget` in `groundedScenarioImpactAssessment.ts`.

Confidence: `supportState` is the presented epistemic signal. Internal numeric confidence is 0 unless an existing engine supplies one. Intensity stays qualitative. Magnitude is omitted.
