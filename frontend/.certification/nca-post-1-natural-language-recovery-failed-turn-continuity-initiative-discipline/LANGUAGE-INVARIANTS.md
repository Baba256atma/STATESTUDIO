# Language invariants

Ordinary manager mode must not expose: journey process blocker, canonical relationship, goal linkage, runtime, resolver, binding, NCA:, MO:, EI:, WATCH.

Explicit architecture questions (`What is NCA:5?`, `what does canonical mean`) are help/technical mode and may use those terms.

Final NCA:6 strip still runs on locked turns unless technical mode is active.
