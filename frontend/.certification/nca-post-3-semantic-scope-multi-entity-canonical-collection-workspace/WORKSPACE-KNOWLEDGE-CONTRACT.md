# Workspace knowledge contract

Read-only runtime:

- Stage labels from `nextRuntimeState.trail` (+ focused subject)
- focused object from `focusedSubject`
- empty trail → Stage currently shows no executive objects (Overview)

Never fabricate Stage contents from conversation history or from the full catalog when the trail is empty.

“What is the Stage?” is NEXORA_PRODUCT. “What is on Stage now?” is CURRENT_WORKSPACE. Mixed turns compose product explanation + actual trail.
