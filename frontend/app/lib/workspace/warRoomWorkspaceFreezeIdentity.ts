/** WS-8:8 — Canonical War Room Workspace Freeze identity. */
export const WarRoomWorkspaceFreezeIdentity = Object.freeze({
  id: "WS-8:8/WarRoomWorkspaceFreeze",
  name: "War Room Workspace Freeze",
  phaseId: "WS-8:8",
  namespace: "nexora.workspace.war-room.freeze",
  version: "1.0.0",
  layer: "Workspace Layer",
  status: "ReadyForPublicIndex",
  readiness: "ReadyForPublicIndex",
  metadataOnly: true,
  immutable: true,
} as const);
