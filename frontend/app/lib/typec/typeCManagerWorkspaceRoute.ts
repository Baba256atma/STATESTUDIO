/** Type-C manager workspace route activation constants (shared by page + domain card). */

import { DEFAULT_LAUNCH_DOMAIN_ID } from "../product/mvpShippingPlan";

/** Manager workspace domain pack used when entering via `/type-c`. */
export const TYPE_C_MANAGER_DOMAIN_ID = DEFAULT_LAUNCH_DOMAIN_ID;

export const TYPE_C_DOMAIN_STORAGE_KEY = "nexora.selected_domain";

export const TYPE_C_GOVERNANCE_ORG_ID = "nexora-type-c";
