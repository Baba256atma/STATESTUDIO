/**
 * NPA-T DTH-EXP:2 — Object Stage Roles identity.
 *
 * Read-only Theatre Actor resolution over existing canonical Objects.
 * Does not own Objects, Stage, Director, VAI, Evidence, or Nexo families.
 */

export const dthExpObjectStageRoleIdentity = "NPA-T DTH-EXP:2/ObjectStageRoles" as const;
export const dthExpObjectStageRoleVersion = "1.0.0" as const;
export const dthExpObjectStageRoleNamespace = "nexora.dth-exp.object-stage-roles" as const;
export const dthExpObjectStageRolePhase = "DTH-EXP:2" as const;
export const dthExpObjectStageRoleArchitecturalRole = "TheatreActorResolutionContract" as const;
