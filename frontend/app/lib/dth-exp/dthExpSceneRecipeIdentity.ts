/**
 * NPA-T DTH-EXP:3A — Scene Recipe System identity.
 *
 * Read/projection instructions for composing Theatre Actors into a management scene.
 * Does not own Objects, Stage, Director, Evidence, VAI, or Nexo family recipes.
 */

export const dthExpSceneRecipeIdentity = "NPA-T DTH-EXP:3A/SceneRecipeSystem" as const;
export const dthExpSceneRecipeVersion = "1.0.0" as const;
export const dthExpSceneRecipeNamespace = "nexora.dth-exp.scene-recipe" as const;
export const dthExpSceneRecipePhase = "DTH-EXP:3A" as const;
export const dthExpSceneRecipeArchitecturalRole = "SceneRecipeProjectionContract" as const;
