/**
 * EXS Stage tokens — aligned to Sprint 2 cockpit language.
 */

import { cockpit } from "./shell/executiveCockpitTheme";

export const exs1 = {
  bg: cockpit.bg,
  graphite: cockpit.graphite,
  charcoal: cockpit.charcoal,
  navy: cockpit.navy,
  panel: cockpit.panel,
  panelSoft: cockpit.panelSoft,
  border: cockpit.border,
  borderStrong: cockpit.borderStrong,
  text: cockpit.text,
  textSoft: cockpit.textSoft,
  muted: cockpit.muted,
  lowMuted: cockpit.lowMuted,
  accent: cockpit.accent,
  accentSoft: cockpit.accentSoft,
  accentGlow: cockpit.accentGlow,
  risk: cockpit.risk,
  success: cockpit.success,
  warning: cockpit.warning,
  stageBg: cockpit.stageBg,
  transition: cockpit.transition,
  elevation: cockpit.elevation,
  radius: cockpit.radius,
  director: cockpit.director,
} as const;
