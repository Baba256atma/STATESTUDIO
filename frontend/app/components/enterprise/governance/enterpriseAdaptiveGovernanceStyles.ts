import type React from "react";

import { nx } from "../../ui/nexoraTheme";

export const enterpriseAdaptiveGovernanceStripStyle: React.CSSProperties = {
  padding: "8px 10px",
  borderRadius: 8,
  border: `1px solid ${nx.border}`,
  background: nx.bgPanelSoft,
  marginBottom: 8,
};
